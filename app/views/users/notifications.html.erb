<div class="container-fluid">
  <div class="row">
    <div class="col-md-3">
    </div>
    <% @notifications.try(:each) do |notification| %>
        <div class="col-md-6 panel">
          <% if notification.eventable_type=='Micropost' %>
              <p>
                <%= notification.user.name %> <%= notification.msg %> <%= link_to 'μBlog', micropost_path(notification.eventable) %>
              </p>
          <% end %>
        </div>
    <% end %>
    <%= will_paginate @notifications %>
    <div class="col-md-3">
      <div class="well"></div>
    </div>
  </div>
</div>